# siyuan-mcp 第二版重设计 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 writing-plans。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 将 sy-last/sy-session 替换为 sy-read/sy-delete，sy-save 回归简单写入，sy-list 改为列文档，新增 sy-notebook。共 6 个低层级确定性工具。

**架构：** 在现有 server.py 基础上修改工具列表和 handlers。删减代码为主（去掉确认流程、内容校验），新增 sy-read 和 sy-delete handler，添加对应的 SiyuanClient API 封装。

**技术栈：** Python 3.10+ / MCP SDK / httpx / jieba

**参考：** [v2 Redesign Spec](../specs/2026-06-11-redesign-v2.md)

---

## 文件结构

| 文件 | 操作 | 职责 |
|------|------|------|
| `siyuan_mcp/siyuan/client.py` | 修改 | 新增 get_doc、remove_doc、list_docs 方法 |
| `siyuan_mcp/siyuan/models.py` | 修改 | 新增 GetDocResponse、DocInfo 模型 |
| `siyuan_mcp/mapper.py` | 保留不变 | NotebookMapper 继续用于笔记本身份解析 |
| `siyuan_mcp/tagger.py` | 保留不变 | 自动标签仍被 sy-save 使用 |
| `siyuan_mcp/server.py` | 修改 | 工具列表重写、handlers 重写、删除确认流程 |
| `tests/test_siyuan_client.py` | 修改 | 新增 get_doc/remove_doc/list_docs 测试 |
| `tests/test_server.py` | 重写 | 适配新 6 工具体系 |

---

## 任务分解

### 任务 1：SiyuanClient 新增 get_doc、remove_doc、list_docs

**文件：**
- 修改：`siyuan_mcp/siyuan/models.py` — 新增 GetDocResponse
- 修改：`siyuan_mcp/siyuan/client.py` — 新增 3 个方法
- 修改：`tests/test_siyuan_client.py` — 新增测试

#### Step 1：添加 GetDocResponse 模型

在 `siyuan_mcp/siyuan/models.py` 末尾添加：

```python
class GetDocResponse(BaseModel):
    """读取文档的响应。"""
    id: str
    content: str = ""
    path: str = ""
    title: str = ""
```

#### Step 2：实现 get_doc 方法

在 `SiyuanClient` 中添加：

```python
async def get_doc(self, doc_id: str) -> GetDocResponse:
    """读取文档内容。"""
    resp = await self._call_api("/api/filetree/getDoc", {"id": doc_id})
    return GetDocResponse(
        id=resp.get("id", doc_id),
        content=resp.get("content", ""),
        path=resp.get("path", ""),
        title=resp.get("title", ""),
    )
```

#### Step 3：实现 remove_doc 方法

```python
async def remove_doc(self, notebook_id: str, path: str) -> bool:
    """删除文档。"""
    await self._call_api("/api/filetree/removeDoc", {
        "notebook": notebook_id,
        "path": path,
    })
    return True
```

#### Step 4：实现 list_docs 方法

```python
async def list_docs(self, notebook_id: str) -> list[dict]:
    """列出笔记本下的文档树。"""
    resp = await self._call_api("/api/filetree/getDocTree", {
        "notebook": notebook_id,
    })
    # 返回文档树列表
    return resp if isinstance(resp, list) else []
```

#### Step 5：编写测试

在 `tests/test_siyuan_client.py` 中添加：

```python
@pytest.mark.asyncio
async def test_get_doc_success(config):
    client = SiyuanClient(config)
    mock_response = {
        "code": 0, "msg": "",
        "data": {
            "id": "doc-123",
            "content": "# 测试内容\n正文",
            "path": "/测试",
            "title": "测试内容",
        },
    }
    with patch.object(client._client, "post") as mock_post:
        mock_post.return_value = AsyncMock()
        mock_post.return_value.status_code = 200
        mock_post.return_value.json = lambda: mock_response
        result = await client.get_doc("doc-123")
        assert result.id == "doc-123"
        assert "测试内容" in result.content


@pytest.mark.asyncio
async def test_remove_doc_success(config):
    client = SiyuanClient(config)
    mock_response = {"code": 0, "msg": "", "data": None}
    with patch.object(client._client, "post") as mock_post:
        mock_post.return_value = AsyncMock()
        mock_post.return_value.status_code = 200
        mock_post.return_value.json = lambda: mock_response
        result = await client.remove_doc("nb-1", "/test/doc")
        assert result is True


@pytest.mark.asyncio
async def test_list_docs_success(config):
    client = SiyuanClient(config)
    mock_response = {
        "code": 0, "msg": "",
        "data": [
            {"id": "doc-1", "title": "文档1", "path": "/文档1"},
            {"id": "doc-2", "title": "文档2", "path": "/文件夹/文档2"},
        ],
    }
    with patch.object(client._client, "post") as mock_post:
        mock_post.return_value = AsyncMock()
        mock_post.return_value.status_code = 200
        mock_post.return_value.json = lambda: mock_response
        result = await client.list_docs("nb-1")
        assert len(result) == 2
        assert result[0]["id"] == "doc-1"
```

#### Step 6：运行测试

```bash
cd d:/Code/siyuan-bridge && python -m pytest tests/test_siyuan_client.py -v
```

#### Step 7：Commit

```bash
git add -A
git commit -m "feat: add get_doc, remove_doc, list_docs to SiyuanClient"
```

---

### 任务 2：server.py 工具重写

**文件：**
- 修改：`siyuan_mcp/server.py`
- 重新：`tests/test_server.py`

这是核心任务。变更量很大但主要是删减代码。

#### Step 1：重写工具列表（handler_list_tools）

保留 `sy-find` 不动。新增/修改 5 个工具：

```python
types.Tool(
    name="sy-notebook",
    description="列出思源笔记中所有可用笔记本（带编号和名称）。",
    inputSchema={"type": "object", "properties": {}},
),
types.Tool(
    name="sy-list",
    description="列出指定笔记本下的文档树。",
    inputSchema={
        "type": "object",
        "properties": {
            "notebook": {"type": "string", "description": "笔记本序号或名称"},
            "path": {"type": "string", "description": "可选，限定目录路径"},
        },
    },
),
types.Tool(
    name="sy-save",
    description="保存文档到思源笔记。",
    inputSchema={
        "type": "object",
        "properties": {
            "content": {"type": "string", "description": "Markdown 内容"},
            "notebook": {"type": "string", "description": "可选，笔记本序号或名称，默认索引0"},
            "path": {"type": "string", "description": "可选，文档路径"},
        },
        "required": ["content"],
    },
),
types.Tool(
    name="sy-read",
    description="读取思源笔记文档内容。",
    inputSchema={
        "type": "object",
        "properties": {
            "id": {"type": "string", "description": "文档 ID"},
        },
        "anyOf": [{"required": ["id"]}],
    },
),
types.Tool(
    name="sy-delete",
    description="删除思源笔记文档。",
    inputSchema={
        "type": "object",
        "properties": {
            "id": {"type": "string", "description": "文档 ID"},
            "notebook": {"type": "string", "description": "笔记本身份（与 id 配合）"},
        },
        "required": ["id"],
    },
),
```

#### Step 2：重写 dispatch 表

```python
handlers = {
    "sy-notebook": _handle_sy_notebook,
    "sy-list": _handle_sy_list,
    "sy-save": _handle_sy_save,
    "sy-read": _handle_sy_read,
    "sy-delete": _handle_sy_delete,
    "sy-find": _handle_sy_find,
}
```

#### Step 3：实现 sy-notebook handler

```python
async def _handle_sy_notebook(args: dict) -> list[types.TextContent]:
    """列出笔记本。"""
    try:
        notebooks = await _siyuan_client.list_notebooks()
        _notebook_mapper.set_notebooks(notebooks)
        return [types.TextContent(type="text", text=_notebook_mapper.format_list())]
    except ConnectionError as e:
        return [types.TextContent(type="text", text=f"❌ {e}")]
    except Exception as e:
        return [types.TextContent(type="text", text=f"❌ 获取笔记本列表失败：{e}")]
```

#### Step 4：重写 sy-list handler（列文档）

```python
async def _handle_sy_list(args: dict) -> list[types.TextContent]:
    """列出笔记本下的文档树。"""
    notebook_spec = args.get("notebook", "")
    try:
        notebook_id = _notebook_mapper.resolve(notebook_spec)
    except ValueError as e:
        notebooks = await _siyuan_client.list_notebooks()
        _notebook_mapper.set_notebooks(notebooks)
        notebook_id = _notebook_mapper.resolve(notebook_spec)

    try:
        docs = await _siyuan_client.list_docs(notebook_id)
        if not docs:
            return [types.TextContent(type="text", text="📭 该笔记本下没有文档")]
        lines = ["📄 文档列表：\n"]
        for d in docs:
            lines.append(f"- {d.get('title', '未命名')}  `{d.get('path', '')}`")
        return [types.TextContent(type="text", text="\n".join(lines))]
    except Exception as e:
        return [types.TextContent(type="text", text=f"❌ 获取文档列表失败：{e}")]
```

#### Step 5：重写 sy-save handler（去掉确认流程）

```python
async def _handle_sy_save(args: dict) -> list[types.TextContent]:
    content = args.get("content", "")
    if not content.strip():
        return [types.TextContent(type="text", text="❌ 内容不能为空")]
    notebook_spec = args.get("notebook", "")
    doc_path = args.get("path", "")

    try:
        notebook_id = _notebook_mapper.resolve(notebook_spec)
    except ValueError:
        notebooks = await _siyuan_client.list_notebooks()
        _notebook_mapper.set_notebooks(notebooks)
        notebook_id = _notebook_mapper.resolve(notebook_spec)

    # 自动生成路径
    if not doc_path:
        from siyuan_mcp.server import _make_doc_path
        doc_path = _make_doc_path(content)

    try:
        result = await _siyuan_client.create_doc(
            markdown=content,
            path=doc_path,
            notebook_id=notebook_id,
        )
        text = f"✅ 已保存\n- 标题：{result.title}"
        if result.id:
            text += f"\n- 🔗 siyuan://blocks/{result.id}"
        return [types.TextContent(type="text", text=text)]
    except ConnectionError as e:
        return [types.TextContent(type="text", text=f"❌ {e}")]
    except Exception as e:
        return [types.TextContent(type="text", text=f"❌ 保存失败：{e}")]
```

#### Step 6：实现 sy-read handler

```python
async def _handle_sy_read(args: dict) -> list[types.TextContent]:
    doc_id = args.get("id", "")
    if not doc_id.strip():
        return [types.TextContent(type="text", text="❌ 文档 ID 不能为空")]

    try:
        doc = await _siyuan_client.get_doc(doc_id)
        return [types.TextContent(type="text", text=doc.content)]
    except ConnectionError as e:
        return [types.TextContent(type="text", text=f"❌ {e}")]
    except Exception as e:
        return [types.TextContent(type="text", text=f"❌ 读取失败：{e}")]
```

#### Step 7：实现 sy-delete handler

```python
async def _handle_sy_delete(args: dict) -> list[types.TextContent]:
    doc_id = args.get("id", "")
    if not doc_id.strip():
        return [types.TextContent(type="text", text="❌ 文档 ID 不能为空")]

    try:
        # 先获取文档路径和所在笔记本
        doc = await _siyuan_client.get_doc(doc_id)
        path = doc.path
        if not path:
            return [types.TextContent(type="text", text="❌ 无法确定文档路径")]
        # 通过笔记本名称反查 notebook_id
        notebook_spec = args.get("notebook", "")
        notebook_id = _notebook_mapper.resolve(notebook_spec)
        await _siyuan_client.remove_doc(notebook_id, path)
        return [types.TextContent(type="text", text=f"✅ 已删除文档\n- 路径：{path}")]
    except ConnectionError as e:
        return [types.TextContent(type="text", text=f"❌ {e}")]
    except Exception as e:
        return [types.TextContent(type="text", text=f"❌ 删除失败：{e}")]
```

#### Step 8：删除不再需要的代码

从 server.py 中删除：
- `sy-last` 和 `sy-session` 工具声明
- 两阶段确认逻辑（confirmed 参数相关代码）
- 内容过短检测
- 对话标记检测（_HAS_DIALOG）
- `_format_doc_result` 函数（已不再使用）
- `_resolve_content` 函数中的文件路径读取（sy-save 不再需要 @ 文件前缀功能——或者保留作为一个便利功能）

**注意：** `_extract_title`、`_sanitize_filename`、`_make_doc_path`、`_match_project` 仍然需要保留，被 sy-save 使用。

#### Step 9：重写测试文件

`tests/test_server.py` 需要完全重写，测试新的 6 个工具。

关键测试覆盖：

```python
# sy-notebook
test_sy_notebook_returns_list       # 正常返回笔记本列表
test_sy_notebook_connection_error   # 连接错误

# sy-list
test_sy_list_docs                   # 列出文档
test_sy_list_empty                  # 笔记本无文档

# sy-save
test_sy_save_success                # 保存成功
test_sy_save_empty_content          # 空内容
test_sy_save_connection_error       # 连接错误

# sy-read
test_sy_read_success                # 读取成功
test_sy_read_empty_id               # ID 为空
test_sy_read_connection_error       # 连接错误

# sy-delete
test_sy_delete_success              # 删除成功
test_sy_delete_empty_id             # ID 为空

# sy-find (保留)
test_sy_find_empty_query            # 空查询（保留）
```

#### Step 10：运行全部测试

```bash
cd d:/Code/siyuan-bridge && python -m pytest -v
```

#### Step 11：Commit

```bash
git add -A
git commit -m "refactor: rewrite to 6 low-level tools (remove sy-last/sy-session, add sy-read/sy-delete)"
```
